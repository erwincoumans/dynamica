SUBDIRS( Serialize )

IF( BUILD_COLLADA )
	SUBDIRS(BulletColladaConverter COLLADA_DOM LibXML)
ENDIF()

IF (BUILD_READ_BLEND_OBSOLETE)
	SUBDIRS(jpeglib)
ENDIF()

IF (BUILD_BULLET_MAYA_DYNAMICA_PLUGIN)

	SUBDIRS(MayaPlugin)

ENDIF(BUILD_BULLET_MAYA_DYNAMICA_PLUGIN)


